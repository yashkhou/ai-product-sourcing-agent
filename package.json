{
  "name": "ai-product-sourcing-agent",
  "version": "0.1.0",
  "description": "Marketplace-agnostic product sourcing intelligence with query planning, normalization, deduplication and evidence-based ranking.",
  "type": "module",
  "engines": {"node": ">=20"},
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "demo": "npm run build && node dist/demo.js",
    "test": "npm run build && node --test dist/tests/*.test.js"
  },
  "devDependencies": {"@types/node": "^26.0.0", "typescript": "^6.0.0"}
}
